<views:BaseUserControl x:Class="Robot.Application.Views.Learning.LearningView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:Robot.Application.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
                       Loaded="LearningView_OnLoaded">    
    <Grid>
        <StackPanel>
            <Label Content="Yes"></Label>
            <Label Content="{Binding Path=ManufacturerId}"></Label>
            <Label Content="{Binding Path=ManufacturerName}"></Label>
            <Label Content="{Binding Path=ModelId}"></Label>
            <Label Content="{Binding Path=ModelName}"></Label>
            <Label Content="{Binding Path=ModelYear}"></Label>
        </StackPanel>
    </Grid>
</views:BaseUserControl>
