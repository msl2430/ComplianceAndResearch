<UserControl x:Class="EngineCell.Application.Views.TestDisplay.TestDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EngineCell.Application.Views"
             xmlns:stripChart="clr-namespace:EngineCell.Application.Views.StripChart"
             xmlns:stripChartViewModel="clr-namespace:EngineCell.Application.ViewModels.StripChart"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:pid="clr-namespace:EngineCell.Application.Views.Pid"
             xmlns:pidModel="clr-namespace:EngineCell.Application.ViewModels.Pid"
             mc:Ignorable="d" 
             Loaded="TestDisplay_OnLoaded">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type stripChartViewModel:StripChartViewModel}">
            <stripChart:StripChart/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type pidModel:PidDisplayModel}">
            <pid:PidDisplay></pid:PidDisplay>
        </DataTemplate>
        <DataTemplate DataType="{x:Type pidModel:DynoThrottlePidViewModel}">
          <pid:DynoThrottlePid></pid:DynoThrottlePid>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="350"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*" MinWidth="200"/>
        </Grid.ColumnDefinitions>
        <Grid Column="0">
          <Grid.RowDefinitions>
            <RowDefinition Height="115"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          <Grid Row="0">
            <ContentControl x:Name="DynoThrottle" Content="{Binding DynoThrottlePid}"></ContentControl>
          </Grid>
          <Grid Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"></ColumnDefinition>
                <ColumnDefinition Width="350"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="75"/>
                <RowDefinition Height="75"/>
                <RowDefinition Height="75"/>
            </Grid.RowDefinitions>
            <!--<ContentControl x:Name="Dyno1" Content="{Binding DynoPid1}" Grid.Row="0" Grid.Column="0"></ContentControl>
            <ContentControl x:Name="Throttle1" Content="{Binding ThrottlePid1}" Grid.Row="0" Grid.Column="1"></ContentControl>-->
            <ContentControl x:Name="Coolant" Content="{Binding CoolantPid}" Grid.Row="0" Grid.Column="0"></ContentControl>
            <ContentControl x:Name="Oil" Content="{Binding OilPid}" Grid.Row="0" Grid.Column="1"></ContentControl>
            <ContentControl x:Name="Intercooler" Content="{Binding Intercooler}" Grid.Row="1" Grid.Column="0"></ContentControl>
            <!--<ContentControl x:Name="Throttle1" Content="{Binding ThrottlePid1}" Grid.Row="2" Grid.Column="1"></ContentControl>-->
            </Grid>
            <!--<controls:MetroAnimatedSingleRowTabControl Margin="5" HorizontalAlignment="Stretch" Width="Auto">
                <TabItem Header="Start" IsSelected="True" Padding="20,0,20,0" BorderBrush="{StaticResource HeadlineBlueBrush}" BorderThickness="0,0,0,1.01">
                    <Grid Margin="0,25,0,0">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <Label Grid.Column="0" VerticalAlignment="Center">Starter Timeout (seconds)</Label>
                                <TextBox Grid.Column="1" Text="30" Width="75" Margin="0,10,0,10" VerticalAlignment="Center"></TextBox>
                            </Grid>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <Label Grid.Column="0" VerticalAlignment="Center">Starter Retries</Label>
                                <TextBox Grid.Column="1" Width="75" Margin="0,10,0,10" VerticalAlignment="Center"></TextBox>
                            </Grid>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <Label Grid.Column="0" VerticalAlignment="Center">Feather Throttle</Label>
                                <Button Grid.Column="1" Width="75" Content="Feather" Margin="0,10,0,10" VerticalAlignment="Center"></Button>
                            </Grid>
                            <Grid>
                                <Button Name="StartPhaseButton" HorizontalAlignment="Center" Width="125" Height="35" Content="Start Phase" Margin="0,30,0,10" Background="{StaticResource DarkBlueBrush}" Click="StartPhaseButton_Click"></Button>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </TabItem>
                <TabItem Header="Warm Up" Padding="20,0,20,0" BorderBrush="{StaticResource HeadlineBlueBrush}" BorderThickness="0,0,0,1.01">
                    <Grid>

                    </Grid>
                </TabItem>

                <TabItem Header="Test" Padding="20,0,20,0" BorderBrush="{StaticResource HeadlineBlueBrush}" BorderThickness="0,0,0,1.01">
                    <Grid>
                        <TabControl TabStripPlacement="Left" Margin="10, 10, 0, 10">

                            <TabItem Header="Phase 1" Padding="0,5,0,5">
                                <TabItem.Content>
                                    <TextBlock TextWrapping="WrapWithOverflow">
                                  FontWeight property information goes here.
                                    </TextBlock>
                                </TabItem.Content>
                            </TabItem>

                            <TabItem Header="Phase 2"  Padding="0,5,0,5">
                                <TabItem.Content>
                                    <TextBlock TextWrapping="WrapWithOverflow">
                                  FontSize property information goes here.
                                    </TextBlock>
                                </TabItem.Content>
                            </TabItem>
                        </TabControl>
                    </Grid>

                </TabItem>
            </controls:MetroAnimatedSingleRowTabControl>-->
        </Grid>
        <GridSplitter Grid.Column="1" Width="Auto" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        <Grid Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="5*"/>
            </Grid.RowDefinitions>
            <Grid Row="0">

            </Grid>
            <Grid Row="1">
                <ContentControl x:Name="StripChartContent" Content="{Binding ChartViewModel}"></ContentControl>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
